services:

  gluetun:
    image: qmcgaw/gluetun:latest
    container_name: gluetun

    cap_add:
      - NET_ADMIN

    restart: unless-stopped

    ports:
      - "8888:8888"
      - "8080:8080"

    volumes:
      - ${CONFIG_ROOT}/gluetun:/gluetun

    environment:
      - TZ=${TZ}

      # Proton VPN
      - VPN_SERVICE_PROVIDER=${VPN_SERVICE_PROVIDER}
      - VPN_TYPE=${VPN_TYPE}
      - WIREGUARD_PRIVATE_KEY=${WIREGUARD_PRIVATE_KEY}
      - WIREGUARD_ADDRESSES=${WIREGUARD_ADDRESSES}
      - SERVER_COUNTRIES=${SERVER_COUNTRIES}

  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent

    network_mode: "service:gluetun"

    depends_on:
      gluetun:
        condition: service_healthy

    restart: unless-stopped

    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
      - WEBUI_PORT=8080

    volumes:
      - ${CONFIG_ROOT}/qbittorrent:/config
      - ${MEDIA_ROOT}/torrents:/downloads

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    container_name: prowlarr

    restart: unless-stopped

    ports:
      - "9696:9696"

    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}

    volumes:
      - ${CONFIG_ROOT}/prowlarr:/config

  sonarr:
   image: lscr.io/linuxserver/sonarr:latest
   container_name: sonarr

   restart: unless-stopped

   ports:
     - "8989:8989"

   environment:
     - TZ=${TZ}
     - PUID=${PUID}
     - PGID=${PGID}

   volumes:
     - ${CONFIG_ROOT}/sonarr:/config
     - ${MEDIA_ROOT}/tv:/tv
     - ${MEDIA_ROOT}/torrents:/downloads

  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr

    restart: unless-stopped

    ports:
      - "7878:7878"

    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}

    volumes:
      - ${CONFIG_ROOT}/radarr:/config
      - ${MEDIA_ROOT}/movies:/movies
      - ${MEDIA_ROOT}/torrents:/downloads


  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    container_name: jellyfin

    restart: unless-stopped

    ports:
      - "8096:8096"

    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}

    volumes:
      - ${CONFIG_ROOT}/jellyfin:/config
      - ${MEDIA_ROOT}/movies:/movies
      - ${MEDIA_ROOT}/tv:/tv
      - ${MEDIA_ROOT}/home-movies:/home-movies
      - ${MEDIA_ROOT}/family-av:/family-av

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    command: tunnel --no-autoupdate run --token eyJhIjoiZGFkODhmNDdjYWU2MzBlODFlOWIwZWM0NTVlNzhmNjIiLCJ0IjoiMmVkNzU2OGMtMjk4OC00OWNjLWIxMWQtMjYxNWJlODY3MWFlIiwicyI6IlkyUmlaRE0yWkdJdFpqTTVaaTAwTVdGbUxXSmhNR0V0TldJeE5HSXhNemt3TURWaiJ9
    restart: unless-stopped


networks:
  default:
    name: kingypi-nas




